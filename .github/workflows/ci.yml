name: CI
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: |
          yarn
          git submodule init
          git submodule update
          yarn start https://uad.ubq.fi
